document.querySelectorAll('[data-copy]').forEach(button=>button.addEventListener('click',async()=>{const text=document.getElementById(button.dataset.copy).textContent;try{await navigator.clipboard.writeText(text);document.getElementById('copy-status').textContent='Copié.';}catch{document.getElementById('copy-status').textContent='Copie indisponible : sélectionnez le texte pour le copier.';}}));
